import { PlaceholderScreen } from "@/components/ui/placeholder-screen";

export default function AccountTab() {
  return (
    <PlaceholderScreen
      title="Account"
      description="Account settings placeholder. Auth UI arrives after the foundation sprint."
      badge="Main Tabs · Account"
    />
  );
}
